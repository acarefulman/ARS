embedding_size: 64
feat_embed_dim: 64

n_layers: [1, 2]
dropout: [0.3, 0.5]
reg_weight: [0.1, 0.01]
cl_weight: 2.0

use_neg_sampling: False
knn_k: 20  # 或其他你希望的 top-k 值
hyper_parameters: ["n_layers", "reg_weight", "dropout"]

